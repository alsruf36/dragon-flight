while(만약 게임오버가 되지 않았다면){
	do(마우스 좌표를 받는다.)
	if(마우스가 움직였다면){
		do(플레이어의 위치를 마우스 좌표로 옮긴다.)
	} 
	
	if(현재 클럭이 bulletClock의 배수라면){
		do(bullet을 생성한다.)
	} 
	
	do(shiftFrame())
	
	if(현재 클럭이 frameClock의 배수라면){
		do(몬스터를 생성한다.) 
	}
	
	do(클럭을 1 증가시킨다.) 
}